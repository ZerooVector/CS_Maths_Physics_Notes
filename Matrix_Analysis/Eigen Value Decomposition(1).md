#MA 
在几何上，特征值分解与一维不变子空间有关：
![[Pasted image 20230303094725.png|600]]
在数学上，我们这样表示特征值分解：
$$
AV = V \Lambda\quad\Lambda = \mathrm{diag}(\lambda_i)
$$
也就是说，在入口基向量和出口基向量相同——都是矩阵的特征值的情况下，线性变换 $\Lambda$ 仅仅表示对于向量的一个伸缩，二不会使得其方向发生变化。

